import axios from 'axios';
export const ApiClient = async (url, header, method, body) => {
    try {
        const config = {
            url: url,
            method: method,
            headers: header,
        };
        if (body && method === 'POST')
            config.data = body;
        const response = await axios(config);
        return response.data;
    }
    catch (error) {
        throw new Error(`Error with response: ${error.message}`);
    }
};
//# sourceMappingURL=apiClient.js.map